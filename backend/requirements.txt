# Phụ thuộc lúc chạy của backend K8s Hub.
#
# Nguồn sự thật vẫn là [project.dependencies] trong pyproject.toml — file này
# là bản chép ra để cài nhanh bằng pip khi không có uv:
#
#     python -m venv .venv
#     .venv/Scripts/activate        # Windows Git Bash
#     pip install -r requirements.txt
#
# Sửa pyproject.toml thì nhớ sửa cả đây.

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sse-starlette>=2.1.3
python-multipart>=0.0.12

# --- Database ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
pgvector>=0.3.6

# --- Cache / queue ---
redis>=5.2.0

# --- LLM / agent ---
# Không khai `anthropic`: langchain-anthropic đã kéo nó theo.
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0.0
langchain-core>=0.3.20
langchain-groq>=0.2.3
langchain-google-genai>=2.0.8
langchain-anthropic>=0.3.0
mcp>=1.1.0

# --- Kubernetes / nguồn dữ liệu quan sát ---
kubernetes-asyncio>=31.1.0
httpx>=0.28.0
pyyaml>=6.0.2

# --- Quan sát LLM/agent (lớp A) ---
langfuse>=3.0.0

# --- Tự giám sát chính app này (lớp C) ---
prometheus-fastapi-instrumentator>=7.0.0
structlog>=24.4.0

# --- Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
